
services:
  nginx:
    image: nginx
    ports:
      - "80:80"
    volumes:
      - .:/app
      - ./docker/nginx/conf.d/bicycleshop.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php


  php:
    build: docker/php
    volumes:
      - .:/app
    working_dir: /app
